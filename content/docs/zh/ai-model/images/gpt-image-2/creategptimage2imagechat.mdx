---
title: GPT Image 2 图像生成（OpenAI 聊天格式）
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          使用 OpenAI Chat Completions 兼容格式 `/v1/chat/completions` 调用 GPT Image 2
          生成图像。直接传 `model`、`messages`、`stream` 即可，默认不需要 `extra_body`。图片通常以
          Markdown data URI 形式返回到 `choices[].message.content`。
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

使用 OpenAI Chat Completions 兼容格式 `/v1/chat/completions` 调用 GPT Image 2 生成图像。直接传 `model`、`messages`、`stream` 即可，默认不需要 `extra_body`。图片通常以 Markdown data URI 形式返回到 `choices[].message.content`。

<APIPage document={"openapi/generated/ai-model/图像（Images）/GPT Image 2/post-v1-chat-completions-creategptimage2imagechat-900100012.json"} operations={[{"path":"/v1/chat/completions","method":"post"}]} />