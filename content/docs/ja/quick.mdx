---
title: クイックスタート
description: 数分で 算力ルーター プラットフォーム API の使い方を学ぶ
icon: Zap
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

## ステップ 1: API キーの取得

算力ルーター プラットフォームのコンソールに登録し、あなたのユニークな API キー（トークン）を生成します。


## ステップ 2: 統合

すでに OpenAI SDK を使用している場合は、基本 URL を変更するだけで済みます。

<Tabs items={['Python', 'Node.js', 'Shell']}>
  <Tab value="Python">
    ```python
from openai import OpenAI

client = OpenAI(
    api_key="YOUR_NODEKEY_KEY",
    base_url="https://nodekey.xinghanyun.cn/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)
    ```
  </Tab>
  <Tab value="Node.js">
    ```ts
import OpenAI from "openai";

const client = new OpenAI({
    apiKey: "YOUR_NODEKEY_KEY",
    baseURL: "https://nodekey.xinghanyun.cn/v1"
});

const completion = await client.chat.completions.create({
    model: "gpt-4o",
    messages: [{ role: "user", content: "Hello!" }],
});
    ```
  </Tab>
  <Tab value="Shell">
    ```bash
curl -X POST "https://nodekey.xinghanyun.cn/v1/chat/completions" \
  -H "Authorization: Bearer YOUR_NODEKEY_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [
      {
        "role": "user",
        "content": "Hello!"
      }
    ]
  }'
    ```
  </Tab>
</Tabs>