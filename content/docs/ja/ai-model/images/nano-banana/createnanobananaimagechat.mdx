---
title: Nano Banana 画像生成（OpenAI チャット形式）
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          OpenAI Chat Completions 互換形式を使用して `gemini-2.5-flash-image`（Nano
          Banana）を呼び出します。プロジェクトは Gemini リクエストに変換されます；直接 `model`、`messages`、`stream` を渡すだけです。`extra_body.google.image_config` は画像の比率やサイズを指定する必要がある場合にのみ使用され、フィールドは snake_case を使用する必要があります。
---

{/* このファイルは Fumadocs によって生成されました。このファイルを直接編集しないでください。変更は生成コマンドを再実行することで行う必要があります。 */}

OpenAI Chat Completions 互換形式を使用して `gemini-2.5-flash-image`（Nano Banana）を呼び出します。プロジェクトは Gemini リクエストに変換されます；直接 `model`、`messages`、`stream` を渡すだけです。`extra_body.google.image_config` は画像の比率やサイズを指定する必要がある場合にのみ使用され、フィールドは snake_case を使用する必要があります。

<APIPage document={"openapi/generated/ai-model/图像（Images）/Nano Banana/post-v1-chat-completions-createnanobananaimagechat-900100003.json"} operations={[{"path":"/v1/chat/completions","method":"post"}]} />