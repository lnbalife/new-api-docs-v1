---
title: Nano Banana 画像生成（Gemini ネイティブフォーマット）
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          `gemini-2.5-flash-image`（Nano Banana）画像生成。リクエストパスの `{model}` は
          `gemini-2.5-flash-image` に設定します；`generationConfig.responseModalities` には
          `IMAGE` を含める必要があり、画像パラメータは `generationConfig.imageConfig` に配置します。
---

{/* このファイルは Fumadocs によって生成されました。このファイルを直接編集しないでください。変更は再度生成コマンドを実行することで行うべきです。 */}

`gemini-2.5-flash-image`（Nano Banana）画像生成。リクエストパスの `{model}` は `gemini-2.5-flash-image` に設定します；`generationConfig.responseModalities` には `IMAGE` を含める必要があり、画像パラメータは `generationConfig.imageConfig` に配置します。

<APIPage document={"openapi/generated/ai-model/图像（Images）/Nano Banana/post-v1beta-models-model-generatecontent-createnanobananaimagegemini-900100002.json"} operations={[{"path":"/v1beta/models/{model}:generateContent","method":"post"}]} />