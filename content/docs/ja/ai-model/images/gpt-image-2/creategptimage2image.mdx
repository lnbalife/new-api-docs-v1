---
title: GPT Image 2 画像生成
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          OpenAIの画像インターフェースを使用して画像を生成します。現在のプロジェクトは
          `gpt-image-2-1k`、`gpt-image-2-2k`、`gpt-image-2-4k` の3つのモデルバリアントをサポートしています；`quality`
          が指定されていない場合はデフォルトで `medium` が使用され、`n` が指定されていないか `0` が指定された場合は `1` として処理されます。
---

{/* このファイルはFumadocsによって生成されました。このファイルを直接編集しないでください。変更は生成コマンドを再実行することで行う必要があります。 */}

OpenAIの画像インターフェースを使用して画像を生成します。現在のプロジェクトは `gpt-image-2-1k`、`gpt-image-2-2k`、`gpt-image-2-4k` の3つのモデルバリアントをサポートしています；`quality` が指定されていない場合はデフォルトで `medium` が使用され、`n` が指定されていないか `0` が指定された場合は `1` として処理されます。

<APIPage document={"openapi/generated/ai-model/图像（Images）/GPT Image 2/post-v1-images-generations-creategptimage2image-900100001.json"} operations={[{"path":"/v1/images/generations","method":"post"}]} />