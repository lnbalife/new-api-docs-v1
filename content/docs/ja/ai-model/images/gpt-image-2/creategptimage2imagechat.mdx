---
title: GPT Image 2 画像生成（OpenAI チャット形式）
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          OpenAI Chat Completions 互換形式 `/v1/chat/completions` を使用して GPT Image 2
          画像を生成します。直接 `model`、`messages`、`stream` を渡すだけで、デフォルトでは `extra_body` は必要ありません。画像は通常、Markdown データ URI 形式で `choices[].message.content` に返されます。
---

{/* このファイルは Fumadocs によって生成されました。このファイルを直接編集しないでください。変更は再生成コマンドを実行することで行うべきです。 */}

OpenAI Chat Completions 互換形式 `/v1/chat/completions` を使用して GPT Image 2 画像を生成します。直接 `model`、`messages`、`stream` を渡すだけで、デフォルトでは `extra_body` は必要ありません。画像は通常、Markdown データ URI 形式で `choices[].message.content` に返されます。

<APIPage document={"openapi/generated/ai-model/图像（Images）/GPT Image 2/post-v1-chat-completions-creategptimage2imagechat-900100012.json"} operations={[{"path":"/v1/chat/completions","method":"post"}]} />