---
title: アリババ万相 Wan 動画生成
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          アリババ通義万相 Wan 動画生成インターフェース。このインターフェースは非同期動画タスクプロセスを経て、提出後にタスク
          IDを返し、その後クエリインターフェースを通じて状態と結果を取得します。テキストから動画、画像から動画をサポート；画像から動画は `input_reference` を通じて最初のフレーム画像を渡すことを推奨し、より多くの上流の
          `input` と `parameters` パラメータは `metadata` を通じて透過的に渡すことができます。
---

{/* このファイルはFumadocsによって生成されました。このファイルを直接編集しないでください。変更は再度生成コマンドを実行することで行うべきです。 */}

アリババ通義万相 Wan 動画生成インターフェース。このインターフェースは非同期動画タスクプロセスを経て、提出後にタスク IDを返し、その後クエリインターフェースを通じて状態と結果を取得します。テキストから動画、画像から動画をサポート；画像から動画は `input_reference` を通じて最初のフレーム画像を渡すことを推奨し、より多くの上流の `input` と `parameters` パラメータは `metadata` を通じて透過的に渡すことができます。

<APIPage document={"openapi/generated/ai-model/视频（Videos）/阿里万相 Wan/post-v1-video-generations-createaliwanvideo-900100008.json"} operations={[{"path":"/v1/video/generations","method":"post"}]} />