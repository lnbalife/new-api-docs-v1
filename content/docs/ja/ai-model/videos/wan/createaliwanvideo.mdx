---
title: アリババ万相 Wan ビデオ生成
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          アリババ通義万相 Wan ビデオ生成インターフェース。このインターフェースは非同期ビデオタスクプロセスを経て、提出後にタスク
          IDを返し、その後クエリインターフェースを通じて状態と結果を取得します。テキストからビデオ、画像からビデオをサポートしています；画像からビデオは `input_reference` を通じて最初のフレーム画像を渡すことを推奨します。その他の上流の
          `input` および `parameters` パラメータは `metadata` を通じて透過的に渡すことができます。
---

{/* このファイルはFumadocsによって生成されました。このファイルを直接編集しないでください。変更は生成コマンドを再実行することで行う必要があります。 */}

アリババ通義万相 Wan ビデオ生成インターフェース。このインターフェースは非同期ビデオタスクプロセスを経て、提出後にタスク IDを返し、その後クエリインターフェースを通じて状態と結果を取得します。テキストからビデオ、画像からビデオをサポートしています；画像からビデオは `input_reference` を通じて最初のフレーム画像を渡すことを推奨します。その他の上流の `input` および `parameters` パラメータは `metadata` を通じて透過的に渡すことができます。

<APIPage document={"openapi/generated/ai-model/视频（Videos）/阿里万相 Wan/post-v1-video-generations-createaliwanvideo-900100008.json"} operations={[{"path":"/v1/video/generations","method":"post"}]} />