---
title: Kling Image to Video
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: |-
          Use Kling to generate videos from images.

          ```bash
          curl "$BASE_URL/kling/v1/videos/image2video" \
            -H "Authorization: Bearer $NODEKEY_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "model_name": "kling-v2-master",
              "image": "https://example.com/first-frame.jpg",
              "prompt": "The camera slowly zooms in as the character looks out the window",
              "duration": "5",
              "mode": "std"
            }'
          ```

          To query the task, use `GET /kling/v1/videos/image2video/{task_id}`.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Use Kling to generate videos from images.

```bash
curl "$BASE_URL/kling/v1/videos/image2video" \
  -H "Authorization: Bearer $NODEKEY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model_name": "kling-v2-master",
    "image": "https://example.com/first-frame.jpg",
    "prompt": "The camera slowly zooms in as the character looks out the window",
    "duration": "5",
    "mode": "std"
  }'
```

To query the task, use `GET /kling/v1/videos/image2video/{task_id}`.

<APIPage document={"openapi/generated/ai-model/视频（Videos）/可灵格式/post-kling-v1-videos-image2video-createklingimage2video-383844641.json"} operations={[{"path":"/kling/v1/videos/image2video","method":"post"}]} />